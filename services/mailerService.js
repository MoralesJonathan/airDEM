const mailerService = { 
    sendCampaign : id => {
        // get campaign id details
        // send campaign
        console.log(`fake email sent nowhere about ${id}`)
    }
};
module.exports = mailerService;